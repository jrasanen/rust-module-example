pub struct Address {
    pub street: String,
    pub city: String,
    pub state: String,
    pub zip: String,
}
pub struct Person {
    pub username: String,
    pub email: String,
    pub address: Address,
}
